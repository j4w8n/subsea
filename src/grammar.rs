#[derive(Debug, PartialEq, Clone)]
pub enum Token {
    Bool(bool),
    Colon,
    Dollar,
    DoubleColon,
    Equals,
    EqualsEquals,
    Greater,
    GreaterEquals,
    Ident(String),
    If,
    IGreater,
    IGreaterEquals,
    ILess,
    ILessEquals,
    ISlash,
    IStar,
    LBrace,
    LBracket,
    LParen,
    Minus,
    Mut,
    NotEquals,
    Number(NumericKind),
    NumberLiteral(String),
    Percent,
    Plus,
    RBrace,
    RBracket,
    RParen,
    Slash,
    Star,
    Text(String),
    Less,
    LessEquals,
    USlash,
    UGreater,
    UGreaterEquals,
    ULess,
    ULessEquals,
    UStar,

    Ampersand,
    Call,
    Comma,
    Const,
    Exit,
    Jmp,
    Literal,
    LocalIdent(String),
    Mem,
    Pointer(String),
    Pop,
    Print,
    Push,
    Register(String),
    Ret,
    Stack,
    Syscall,
}

#[derive(Debug, PartialEq, Clone)]
pub enum NumericKind {
    I8(i8),
    I16(i16),
    I32(i32),
    I64(i64),
    Int(i64),
    U8(u8),
    U16(u16),
    U32(u32),
    U64(u64),
    F32(f32),
    F64(f64),
    Float(f64),
}
